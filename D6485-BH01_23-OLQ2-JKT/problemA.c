#include <stdio.h>

int main(int argc, char const *argv[])
{
    int num;
    scanf("%d", &num);
    printf("%d plus %d is %d\n", num, num, num+num);
    printf("minus one is %d\n", num-1);
    return 0;
}
